Discription:
-Takes a package file and extracts it to a temporary directory.
-It then runs ninka on every extracted file and stores the content of the files outputed by ninka in a sqlite database.

Operation:

-Move ninka-wrapper.pl to root directory of ninka
-Execute using syntax "perl ninka-wrapper.pl <path to package file> <databasename>"
